{
  "name": "svg-viewer-notion-sync",
  "version": "0.3.0",
  "private": true,
  "description": "Render `svg`/`xml` code blocks as inline previews and sync Notion imports into timestamped folders.",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.17.47",
    "esbuild": "^0.25.3",
    "obsidian": "^1.8.10",
    "tslib": "^2.8.1",
    "typescript": "^5.8.3"
  }
}
