# SVG Viewer & Notion Sync

![Version](https://img.shields.io/github/manifest-json/v/guohuanguohuan/svg-viewer-and-notion-sync?color=blue&label=version)
![License](https://img.shields.io/github/license/guohuanguohuan/svg-viewer-and-notion-sync)
![Obsidian](https://img.shields.io/badge/Obsidian-v1.5.0+-purple)

[English Documentation](#english-preview) | [中文说明](#中文预览)

---

## English Preview

### 🚀 Key Features
- **SVG Preview**: Renders `svg` and `xml` code blocks as responsive inline graphics.
- **Notion Sync**: Automates Notion content import into timestamped folders via the [Importer](https://obsidian.md/plugins?id=obsidian-importer) plugin.

### 🛠️ How It Works (Principles)
#### 1. SVG Rendering
- **Parser**: Uses `DOMParser` to convert code block source into live SVG elements.
- **Auto-Correction**: Automatically fixes invalid `xmlns="<url>"` formats (often generated by Notion API/Importer) by stripping the extra `< >` brackets.
- **Sanitization**: Implements a strict whitelist-based sanitizer to strip dangerous tags (e.g., `<script>`, `<iframe>`) and event handlers.
- **Responsiveness**: Automatically injects `viewBox` (if missing) and removes fixed `width`/`height` to ensure SVGs scale with your notes.
- **UI Enhancement**: Adds a checkerboard background to easily identify transparency.

#### 2. Notion Synchronization
- **Plugin Bridge**: Acts as an automation layer for the official **Importer** plugin, programmatically triggering the `Notion (API)` workflow.
- **Versioning**: Each sync creates a unique `run-YYYYMMDD-HHMMSS` folder, serving as an immutable snapshot.
- **Snapshot Management**: Automatically retains only the **latest 5 versions** to optimize storage.

### 📖 Quick Start
1. **SVG**: Wrap your code in ` ```svg ` or ` ```xml ` blocks. 
   - *Tip*: Use `xml` for SVG code in Notion to ensure the Importer treats it correctly.
2. **Notion**: 
   - Install the **Importer** plugin.
   - Enter your **Notion API Token** in settings.
   - Run command: `Sync Notion now`.

---

## 中文预览

### 🚀 核心功能
- **SVG 预览**：将 `svg` 和 `xml` 代码块直接渲染为响应式图形。
- **Notion 同步**：通过 [Importer](https://obsidian.md/plugins?id=obsidian-importer) 插件自动将 Notion 内容导入到带时间戳的文件夹中。

### 🛠️ 工作原理
#### 1. SVG 渲染机制
- **解析器**：使用 `DOMParser` 将代码块源码实时解析为 SVG 元素。
- **格式纠错**：自动识别并清理 Notion API 同步时可能产生的 `xmlns="<url>"` 非法格式（剥离多余的 `< >` 符号），解决无法渲染为图片的问题。
- **安全性**：内置基于白名单的清理器，自动剥离危险标签（如 `<script>`）和事件处理器（如 `onclick`）。
- **响应式适配**：自动补全 `viewBox` 并移除固定宽高，确保图形随笔记容器自适应缩放。
- **UI 优化**：添加棋盘背景，方便识别透明区域。

#### 2. Notion 同步逻辑
- **插件桥接**：作为官方 **Importer** 插件的自动化层，通过编程方式调用其 `Notion (API)` 导入流程。
- **版本控制**：每次同步生成独立的 `run-YYYYMMDD-HHMMSS` 文件夹，作为内容快照。
- **容量管理**：自动保留**最近 5 个版本**，过旧的版本将被清理以节省空间。

### 📖 快速上手
1. **SVG**：将代码放入 ` ```svg ` 或 ` ```xml ` 代码块。
   - **提示**：在 Notion 侧建议将 SVG 代码块语言设置为 `xml`，以获得最佳同步效果。
2. **Notion**：
   - 安装 **Importer** 插件。
   - 在设置中填写 **Notion API Token**。
   - 执行命令：`立即同步 Notion`。

---

## Configuration / 配置

| Setting (英文) | 说明 (Chinese) | Default |
| :--- | :--- | :--- |
| **Notion API Token** | Notion 集成令牌 | - |
| **Auto Sync on Startup** | 启动时自动同步 | Enabled |
| **Import Base Folder** | 导入根目录 | `Notion imports` |
| **Version Count** | 保留版本数 | 5 |

## Installation / 安装

Available via **Obsidian Community Plugins**. Search for "SVG Viewer & Notion Sync".

## Development / 开发

```bash
npm install && npm run build
```

---

**License**: MIT

